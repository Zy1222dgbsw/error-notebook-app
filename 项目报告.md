# 智能错题本 — 编程课程项目报告

> **项目名称**：智能错题本（Smart Error Notebook）  
> **在线体验**：https://Zy1222dgbsw.github.io/error-notebook-app/  
> **GitHub 仓库**：https://github.com/Zy1222dgbsw/error-notebook-app  
> **技术栈**：HTML5 + CSS3 + JavaScript + 硅基流动 API (Qwen3-VL + DeepSeek-V3) + KaTeX + marked.js  
> **开发周期**：2026年7月

---

## 一、项目概述

### 1.1 解决的问题

学生在日常学习中会积累大量错题，传统的手动抄录方式存在以下痛点：

- **耗时费力**：每道错题都要手写抄录，效率极低
- **整理困难**：抄在纸上的错题难以按学科、题型分类和检索
- **缺乏深度理解**：抄完题目后往往不知道错在哪里、为什么错
- **复习不便**：纸质错题本容易丢失、难以携带
- **公式难录入**：数学、物理等科目包含大量公式，手抄极易出错

本项目的目标是：**用手机拍一张错题照片，AI 大模型识别文字（含公式）、实时渲染 Markdown 和 LaTeX 公式、AI 给出详细解答、按学科和题型自动分类存储**——将错题管理的完整流程数字化、智能化。

### 1.2 功能一览

| 功能模块 | 说明 |
|----------|------|
| 📸 拍照 / 🖼️ 相册 | 上传图片，支持裁剪选取题目区域 |
| 🤖 AI 大模型识别 | 接入硅基流动 API，使用 Qwen3-VL 系列视觉大模型识别文字，支持中英文 + 数学公式 |
| 📐 Markdown + 公式渲染 | 识别结果支持 Markdown 格式和 LaTeX 数学公式实时渲染预览 |
| ✏️ 编辑确认 | 识别后可修改文字，支持编辑/预览模式切换 |
| 🏷️ 题型分类 | 选择题/填空/解答/证明/计算……每学科专属题型 |
| 🧠 AI 解答 | 使用 DeepSeek-V3 大模型，给出正确答案、解题思路、知识点和易错提醒，支持公式渲染 |
| 📚 学科管理 | 新建/编辑/删除学科，自定义名称+颜色+题型，按学科整理错题 |
| 🔎 搜索筛选 | 按学科筛选、关键词搜索错题 |
| 🖨️ 打印错题 | 勾选错题 → 一键生成 A4 打印稿，含 AI 解答和答题空白区，支持公式渲染 |
| 🌙 深色模式 | 自动跟随系统偏好，也可手动切换 |
| 📱 手机安装 | PWA 支持，可添加到手机桌面像原生 App 一样使用（含内置安装指南） |
| 💾 本地存储 | 所有数据保存在浏览器 localStorage，无需后端服务器 |
| ⚙️ 多 AI 源 | 默认硅基流动，可选 Pollinations AI / DeepSeek / 自定义接口 |

---

## 二、所用工具与技术

### 2.1 核心技术

| 技术 | 版本 | 用途 |
|------|------|------|
| HTML5 | — | 页面结构，包括摄像头调用（`<video>`、`<canvas>`） |
| CSS3 | — | 响应式样式 + 深色模式变量体系 + 渲染内容样式 |
| JavaScript (ES6+) | — | 核心业务逻辑 |
| 硅基流动 API | v1 | AI 大模型识别与解答的 API 服务 |
| Qwen3-VL-32B-Instruct | — | 主视觉识别模型，320亿参数，支持公式输出 |
| DeepSeek-V3 | — | AI 解答模型，推理能力强，中文理解优秀 |
| KaTeX | 0.16.10 | LaTeX 数学公式渲染 |
| marked.js | 14.1.1 | Markdown 解析与渲染 |
| Service Worker | — | PWA 离线缓存 |
| Web App Manifest | — | 手机桌面安装配置 |
| localStorage | — | 数据本地持久化 |
| Git + GitHub | — | 版本控制与代码托管 |
| GitHub Pages | — | 免费静态网站部署 |

### 2.2 为什么选择这些技术

- **纯前端方案**：不需要服务器，一个 `index.html` 就能在浏览器中运行，降低使用门槛
- **硅基流动 API**：国内访问速度快，提供丰富的开源大模型，免费额度充足
- **Qwen3-VL 系列视觉大模型**：相比传统 OCR 引擎（如 Tesseract.js），大模型不仅能识别文字，还能理解公式结构并输出 LaTeX 格式，准确率显著提升
- **DeepSeek-V3**：当前最强的开源推理模型之一，数学解题能力强，中文输出自然
- **KaTeX**：最快的 Web 数学公式渲染库，支持行内公式和独立公式
- **marked.js**：轻量级 Markdown 解析器，让识别结果和 AI 解答以结构化格式呈现
- **PWA 技术**：Service Worker + Manifest 让网页可以像 App 一样安装到手机
- **localStorage**：简单可靠，数据完全在用户设备上，隐私安全
- **原生 JavaScript**：无需框架学习成本，代码清晰可读

### 2.3 从 OCR 到大模型识别的技术演进

本项目经历了重要的技术升级：

| 维度 | 旧方案 | 新方案 |
|------|--------|--------|
| 识别引擎 | Tesseract.js（传统 OCR） | Qwen3-VL-32B（视觉大模型） |
| 运行方式 | 浏览器端本地运行 | 云端 API 调用 |
| 公式支持 | 不支持 | 支持 LaTeX 格式输出 |
| 识别精度 | 一般（复杂排版易出错） | 高（大模型理解上下文） |
| 首次加载 | 需下载 30MB 语言包 | 无需下载，直接 API 调用 |
| AI 解答模型 | Qwen2.5-7B-Instruct | DeepSeek-V3（推理能力更强） |
| 内容渲染 | 纯文本 | Markdown + LaTeX 公式渲染 |

---

## 三、开发过程

### 3.1 开发阶段

项目采用**迭代式开发**，从基础框架到完整产品共经历多轮迭代：

| 版本 | 主要工作 |
|------|----------|
| v0.1 | 搭建 HTML/CSS/JS 项目框架，设计四模块 UI 布局 |
| v0.2 | 集成 Tesseract.js，实现拍照上传与 OCR 文字识别 |
| v0.3 | 接入 AI 解答功能，支持多 AI 提供商切换 |
| v0.4 | 添加学科管理与 localStorage 数据持久化 |
| v0.5 | OCR 引擎优化、AI 超时重试、移动端适配 |
| v0.6 | 深色模式 + PWA 手机安装支持 |
| v0.7 | 题型分类 + 自定义题型 + OCR 编辑确认 + 双通道上传 |
| v0.8 | **AI 大模型替换 OCR**：接入硅基流动 Qwen3-VL 系列视觉大模型 |
| v0.9 | **公式与 Markdown 渲染**：集成 KaTeX + marked.js，全场景支持渲染 |
| v1.0 | **AI 解答模型升级**：从 Qwen2.5-7B 升级为 DeepSeek-V3 |

### 3.2 Git 提交历史

项目通过 Git 进行版本控制，提交历史完整记录了开发过程和团队协作。

**Git 提交统计**：
- 总提交次数：**30+ 次**
- 修改文件数：**8 个**
- 代码总行数：约 **2500+ 行**（含 CSS、JS、HTML）

### 3.3 项目讨论与调整记录

在开发过程中，项目经历了以下关键讨论和决策调整：

**讨论 #1：OCR 引擎选型**
- 初始方案：使用百度 OCR API（需要后端 + API Key）
- 调整原因：简化部署，降低使用门槛
- 最终方案：Tesseract.js 浏览器端 OCR，无需后端

**讨论 #2：AI 服务选择**
- 初始方案：仅接入 DeepSeek（需要注册 + 付费）
- 调整原因：课程项目应降低使用门槛
- 最终方案：默认使用免费 Pollinations AI，支持 DeepSeek / 自定义接口

**讨论 #3：数据存储方案**
- 初始方案：搭建后端数据库
- 调整原因：非计算机专业，减少复杂度，保护隐私
- 最终方案：localStorage 浏览器本地存储

**讨论 #4：移动端体验**
- 发现问题：手机上按钮太小，不易点击；网页无法离线使用
- 调整：全宽按钮布局 + PWA 离线缓存 + 手机桌面安装

**讨论 #5：题型系统设计**
- 初始方案：固定题型列表
- 调整原因：用户需要自定义学科和题型
- 最终方案：每学科独立题型 + 支持逗号分隔自定义输入

**讨论 #6：CDN 选型**
- 发现问题：jsdelivr.net 在国内被墙，页面白屏
- 调整：切换到 npmmirror 国内镜像 + 多 CDN 自动降级

**讨论 #7：从 OCR 到大模型识别（关键升级）**
- 核心挑战：传统 OCR 引擎（Tesseract.js）无法识别数学公式，且复杂排版准确率低
- 卡住的地方：Tesseract.js 对手写体和印刷体混合的识别效果很差，尤其是上下标、分数、积分等数学符号
- 做出的调整：用硅基流动 API 调用 Qwen3-VL-32B 视觉大模型替代 Tesseract.js
- 解决方案：设计多模型降级策略（Qwen3-VL-32B → Qwen3-VL-30B-A3B → Qwen3-VL-8B → GLM-4.5V），优化提示词要求输出 LaTeX 格式公式

**讨论 #8：公式与 Markdown 渲染支持**
- 核心挑战：识别出的文字包含 LaTeX 公式和 Markdown 标记，但页面以纯文本显示，公式无法正确渲染
- 卡住的地方：KaTeX 的 renderMathInElement 需要在 DOM 元素插入后才能执行，时序控制困难
- 做出的调整：统一封装 renderContentToElement 函数，在所有内容渲染场景（OCR 预览、错题列表、AI 解答、打印）中调用
- 解决方案：使用 setTimeout 延迟渲染 + querySelectorAll 遍历所有需要渲染的容器，确保公式在任何场景下都能正确显示

**讨论 #9：测试 API Key 按钮点击无反应**
- 核心挑战：在设置弹窗中点击「测试 API Key」按钮后，期望看到模型可用性测试结果，但实际没有任何视觉反馈
- 卡住的地方：HTML 元素 `<div id="apiTestResult" style="display:none">` 使用了**内联样式** `display:none`，其 CSS 优先级高于 JS 设置的 `element.hidden = false`。即使 JS 把 `hidden` 属性设为 false，内联 `display:none` 仍然生效，结果区始终隐藏
- 做出的调整：放弃通过 `hidden` 属性控制显隐，改用 `element.style.display = 'block'`
- 解决方案：使用 `result.style.display = 'block'` 显式设置内联样式。同时为 showToast 函数增加 `info` 类型（蓝色提示），延长显示时间到 4 秒，让用户有足够时间查看测试进度

**讨论 #10：打印选中按钮被浏览器拦截**
- 核心挑战：点击「打印选中」后没有反应，无法生成打印预览
- 卡住的地方：原实现使用 `window.open()` 打开新窗口打印，现代浏览器对 `window.open` 的拦截策略非常严格，即使在用户点击事件中调用也可能被拦截；此外 `formatAIAnswer` 函数内部有 `setTimeout` 调用，尝试在打印页查找 `$('answerContent')` 元素，但新窗口中根本不存在该元素
- 做出的调整：放弃 `window.open` 方案，改用**隐藏 iframe** + `iframe.contentWindow.print()` 打印
- 解决方案：创建零尺寸的 iframe 元素并附加到 body，将打印 HTML 写入 iframe 文档，调用 `iframe.contentWindow.print()` 触发打印。打印完成后延迟 1 秒移除 iframe。打印页面的 AI 解答使用内联的 marked.parse 直接转换，不调用可能出错的 formatAIAnswer

**讨论 #11：移动端打印失效 + 浏览器缓存导致旧代码**
- 核心挑战：用户反馈在手机上点击「打印选中」仍无反应，同时 OCR 识别错误显示的仍是旧模型 `Pro/Qwen/Qwen2-VL-7B-Instruct`，但实际 main 分支的代码已经更新
- 卡住的地方：经过排查发现两个独立问题：(1) iOS Safari 等移动浏览器对 0×0 隐藏 iframe 的 `contentWindow.print()` 支持不佳；(2) GitHub Pages + 浏览器 HTTP 缓存的组合使得旧版本的 `app.js` 仍然在用户设备上运行，即使服务端代码已更新
- 做出的调整：
  1. 打印方案改为**三段式降级**：Blob URL + iframe → 失败时直接在当前页面显示打印内容（带显眼的「🖨️ 点击打印 / 保存为 PDF」按钮）→ 用户手动点按钮调用 `window.print()`
  2. 引入**版本检测机制**：在 `app.js` 中定义 `const APP_VERSION = '15'`，每次启动检查 localStorage 中的版本号，不一致时提示用户已更新
  3. 升级 `index.html` 中所有资源 URL 的版本参数 `v=14 → v=15`，强制浏览器重新下载
- 解决方案：
  1. 移动端打印：保存当前页面 body 内容 → 替换为打印 HTML → 添加「返回错题本」浮动按钮 → 用户在当前页面点击打印按钮
  2. 缓存破除：`?v=15` 参数 + localStorage 版本号双重保险
  3. 用户体验：版本号变化时自动显示 toast「检测到新版本 v15，已自动更新」

### 3.4 遇到的问题与解决方案

| 遇到的问题 | 解决方案 | 学到的东西 |
|------------|----------|------------|
| jsdelivr CDN 国内被墙，页面白屏 | 切换 npmmirror 国内镜像 + 多 CDN 自动降级 | CDN 选型要考虑地域可用性 |
| `[hidden]` 属性被 CSS 覆盖 | 加 `[hidden] {display:none !important}` | CSS 权重优先级规则 |
| Tesseract.js 无法识别数学公式 | 替换为 Qwen3-VL 视觉大模型 | 大模型 vs 传统 OCR 的优劣 |
| 旧版视觉模型被硅基流动下架 | 升级为 Qwen3-VL 系列最新模型 | API 模型版本管理与降级策略 |
| LaTeX 公式在动态生成的内容中不渲染 | 封装统一的渲染函数，在内容插入后调用 KaTeX | DOM 操作与异步渲染的时序控制 |
| 手机拍照图片过大（>5MB） | Canvas 压缩为 JPEG 格式 | 客户端图片压缩技术 |
| AI API 可能因网络问题超时 | 添加超时控制 + 多模型自动降级 | 网络请求的鲁棒性设计 |
| Service Worker 缓存旧页面 | 升级缓存版本号 + activate 清理旧缓存 | PWA 缓存策略管理 |
| Markdown 表格在错题卡片中溢出 | 添加 overflow-x: auto 样式 | 响应式布局与内容渲染 |

---

## 四、成果展示

### 4.1 项目界面

**主界面 — 拍照录入**
- 顶部导航栏，三个标签页切换（拍照录入 / 错题本 / 学科管理）
- 上传区域支持拍照、相册选择、拖拽、剪贴板粘贴
- 图片裁剪功能（全屏裁剪弹窗，支持四角拖拽调整）
- AI 大模型识别进度条，显示当前调用的模型名称
- 识别结果可点击编辑，支持「编辑/预览」模式切换
- 预览模式实时渲染 Markdown 和 LaTeX 公式

**错题本 — 列表视图**
- 按学科筛选 + 关键词搜索 + 全选/打印按钮
- 错题卡片：复选框、学科标签（彩色）、题型标签、日期、图片
- 题目文字支持 Markdown 和公式渲染
- AI 解答支持公式渲染，结构化展示
- 勾选错题 → 一键生成 A4 打印稿（含渲染后的公式），移动端友好：直接在当前页面显示打印预览，配「点击打印 / 保存为 PDF」按钮和「返回错题本」按钮

**学科管理**
- 增/改/删：名称 + 颜色选择器 + 题型（逗号分隔）
- 每学科独立的题型列表
- 学科编辑弹窗支持修改现有信息

**AI 解答弹窗**
- 显示原始题目（支持 Markdown 渲染）
- 加载动画
- 结构化解答：正确答案、解题思路、知识点、易错提醒
- 全部支持 LaTeX 公式渲染

**设置面板**
- AI 提供商选择：硅基流动（推荐）/ Pollinations / DeepSeek / 自定义
- API Key 输入（密码框）
- API Key 测试功能（自动测试所有可用模型）

### 4.2 代码量统计

| 文件 | 说明 |
|------|------|
| index.html | 页面结构 + CDN 引入（KaTeX、marked.js） |
| style.css | 响应式样式 + 深色模式 + 渲染内容样式 |
| app.js | 全部业务逻辑（AI 识别 + 渲染 + AI 解答） |
| manifest.json | PWA 配置 |
| sw.js | Service Worker 离线缓存 |
| README.md | 项目文档 |
| .gitignore | 版本控制配置 |
| 项目报告.md | 课程报告 |
| **合计** | **8 个文件，~2500 行** |

### 4.3 项目可运行性

- ✅ 无需安装任何软件
- ✅ 无需配置服务器
- ✅ 双击 `index.html` 即可本地运行
- ✅ GitHub Pages 在线部署，公开可访问
- ✅ 手机浏览器可直接使用
- ✅ 支持 PWA 安装到手机桌面
- ✅ AI 识别和解答需联网（调用硅基流动 API）
- ✅ 所有本地数据离线可用


## 五、总结与反思

### 5.1 项目收获

1. **技术层面**：学会了大模型 API 调用、视觉模型集成、LaTeX 公式渲染、Markdown 解析、PWA 离线缓存
2. **工程层面**：理解了版本控制（Git）的重要性，体验了迭代式开发流程，学会了 Bug 定位和修复
3. **产品层面**：从用户需求出发设计功能，关注移动端体验，考虑隐私和可用性
4. **调试能力**：通过多次问题修复，积累了丰富的调试经验

#### ✅ 解决的问题

本项目解决了学生手动抄录错题的低效问题。通过"拍照 → AI 大模型识别 → Markdown/公式渲染 → AI 解答 → 学科+题型分类"的自动化流程，将原本需要 5-10 分钟的错题整理压缩到 30 秒以内。更重要的是，AI 不仅给出答案，还解释解题思路，帮助学生真正理解错题。公式的自动识别和渲染解决了数学、物理等科目错题数字化的最大障碍。

#### ✅ 收获

1. **大模型替代传统 OCR**：Qwen3-VL 视觉大模型不仅能识别文字，还能理解公式结构并输出 LaTeX，彻底解决了传统 OCR 无法处理数学公式的问题
2. **多模型降级策略**：设计 4 级模型降级链（Qwen3-VL-32B → 30B-A3B → 8B → GLM-4.5V），确保在单个模型不可用时仍能提供服务
3. **统一渲染架构**：封装 renderContentToElement 函数，在 OCR 预览、错题列表、AI 解答、打印输出四个场景统一使用，确保公式在任何地方都能正确渲染
4. **DeepSeek-V3 解题能力**：升级后的 AI 解答模型在数学推理和中文表达方面表现优异
5. **PWA 让网页变 App**：Service Worker + Manifest 让手机浏览器可以「添加到主屏幕」
6. **CSS 权重陷阱**：一个 `display:flex` 覆盖了 HTML 的 `hidden` 属性，导致弹窗关不掉
7. **CDN 地域差异**：jsdelivr 在国内被墙，npmmirror 国内镜像可用

### 5.2 未来改进方向

- 添加错题导出功能（PDF/图片格式）
- 支持手写体 OCR 识别（需要更专业的模型）
- 添加错题复习模式（随机出题、错题统计）
- 实现多设备同步（引入后端数据库）
- 添加错题标签和难度评级
- 支持更多学科专属功能（如英语听力、化学方程式）

### 5.3 结语

这个项目完整地展示了"发现问题 → 分析需求 → 选择技术 → 逐步实现 → 迭代优化"的软件工程流程。从最初的 Tesseract.js 传统 OCR，到最终接入 Qwen3-VL 视觉大模型 + DeepSeek-V3 推理模型 + KaTeX 公式渲染，每一次升级都解决了真实的用户痛点。它在满足评估标准的同时，也产出了一个真正可用的工具。
